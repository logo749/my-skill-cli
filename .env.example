# 登录页面 URL
LOGIN_URL=https://example.com/login

# 服务端口（默认 3000）
PORT=3000

# 登录状态检测接口（可选，不配置则不检测）
# 当检测接口返回非 200 状态时，认为用户已登出
LOGIN_STATUS_URL=https://example.com/api/check-login

# 登录状态检测间隔（毫秒），默认 30000
LOGIN_STATUS_INTERVAL=30000
